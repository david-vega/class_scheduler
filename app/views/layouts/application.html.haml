!!!
%html
  %head
    =render 'shared/head'
  %body
    #main-wrapper
      %header#main-header
        = render 'shared/header'

      %section#main.row
        = render 'api/v1/search/form'
        = yield

      %footer#main-footer
        = render 'shared/footer'

    = yield :script
